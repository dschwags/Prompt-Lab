# Cloudflare Pages Configuration
# For deploying Prompt Lab to Cloudflare Pages

name = "prompt-lab"
compatibility_date = "2024-01-01"

# Build settings for Cloudflare Pages
[build]
command = "npm run build"
cwd = "/"

[build.environment]
NODE_VERSION = "20"
